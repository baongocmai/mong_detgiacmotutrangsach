# Project Config
PROJECT_NAME="Story API"
VERSION="1.0.0"
API_PREFIX="/api"
DEBUG=True

# PostgreSQL Config
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=story_db
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_URI=postgresql+asyncpg://postgres:postgres@db:5432/story_db

# JWT Config
JWT_SECRET_KEY="your-secret-key-here"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Redis Config (có thể bỏ qua nếu chưa dùng)
REDIS_HOST="localhost"
REDIS_PORT=6379
REDIS_PASSWORD=""
REDIS_DB=0
REDIS_TIMEOUT=5

# Email Config (có thể bỏ qua nếu chưa dùng)
MAIL_USERNAME=""
MAIL_PASSWORD=""
MAIL_FROM=""
MAIL_PORT=587
MAIL_SERVER=""

# Cloudinary Config (có thể bỏ qua nếu chưa dùng)
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Firebase Config (có thể bỏ qua nếu chưa dùng)
FIREBASE_CREDENTIALS_PATH=""